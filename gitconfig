# This is Git's per-user configuration file
# Edit the parameters
[user]
	name = Victoria Rudakova
	email = vicrucann@protonmail.com
[core]
    autocrlf = input
    editor = vim
[github]
    user = vicrucann
[color]
    ui = true
    diff = auto
    status = auto
    branch = auto
    interactive = auto
    pager = true
[alias]
    # show all branches
    br = branch -av
    # update all submodules
    subup = submodule update --init
    # list all tags
    tags = tag -l
